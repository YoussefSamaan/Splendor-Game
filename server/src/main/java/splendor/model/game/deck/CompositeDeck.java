package splendor.model.game.deck;

/**
 * A composite deck is a deck that contains multiple decks.
 * It represents the decks on the board.
 */
public interface CompositeDeck extends Iterable<SplendorDeck> {
}
